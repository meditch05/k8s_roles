apiVersion: v1
kind: PersistentVolume
metadata:
  name: accordion-config-pv
  labels:
   volume-task: accordion-config
  annotations:
   # pv.beta.kubernetes.io/gid: "1001001"
spec:
  capacity:
    storage: 10Mi
  accessModes:
    - ReadWriteMany
  nfs:
    server: {{ server }}
    path: {{ configpath }}
